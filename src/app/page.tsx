import Login from '@/components/Login/Login';
import React from 'react';

const page = () => {
    return (
        <div className="">
            <Login />
        </div>
    );
};

export default page;